import { HideIfLocked } from './hide-if-locked';

describe('HideIfLocked', () => {
  it('should create an instance', () => {
    const directive = new HideIfLocked();
    expect(directive).toBeTruthy();
  });
});
